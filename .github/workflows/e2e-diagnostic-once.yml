name: One-time E2E diagnostic

on:
  push:
    branches: [agent/initial-implementation]

permissions:
  contents: write

jobs:
  diagnose:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/initial-implementation
      - uses: pnpm/action-setup@v4
        with:
          version: 11.12.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - run: pnpm exec playwright install --with-deps chromium
      - name: Capture E2E output
        run: |
          pnpm test:e2e > docs/E2E_DIAGNOSTIC.txt 2>&1 || true
          rm .github/workflows/e2e-diagnostic-once.yml
      - name: Commit diagnostic
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add .
          git commit -m "chore: capture remaining E2E diagnostic"
          git push origin HEAD:agent/initial-implementation
